{
  "name": "synexis-sign-mcp",
  "version": "1.1.0",
  "description": "APOSENTADO (2026-08-03) — use o MCP embutido da plataforma em POST /mcp. Servidor MCP standalone que wrappeava a API REST do Synexis Sign.",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "synexis-mcp": "dist/server.js"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["mcp", "synexis", "assinatura", "digital", "signature"],
  "author": "Synexis Tech",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
